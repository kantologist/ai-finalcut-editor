# Suggested first push (run when ready — do not commit secrets):
#
#   git add -A
#   git status   # confirm .env is NOT listed
#   git commit -m "Initial open-source release: Mac desktop app + CLI"
#   gh repo create ai-finalcut-editor --public --source=. --remote=origin --push
#
# Or create the empty GitHub repo first, then:
#   git remote add origin git@github.com:kantologist/ai-finalcut-editor.git
#   git push -u origin main
